# Structure shell

Extracted from `/Users/gcastellazzi/Dropbox/PETRAS_exchange/Project_PETRAS_Benchmark` without binary payloads.
Large JSON-LD arrays are replaced with `_stripped` stubs.
Use this tree for connectivity maps and format demos only.

to_petras=True  strip=True
